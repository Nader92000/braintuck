apiVersion: apps/v1
kind: Deployment
metadata:
  name: myapp
  namespace: devops-tools
spec:
  replicas: 1
  selector:
    matchLabels:
      myapplication: myapp
  template:
    metadata:
      labels:
        myapplication: myapp
    spec:
      containers:
        - name: myapp
          image: app:1
          ports:
            - containerPort: 1234
          volumeMounts:
            - name: data-logs
              mountPath: /home/application/brain-tuck/logs
      nodeName: mas
      volumes:
         - name: data-logs
           hostPath:
              path: /home/nader/app
